name: Rollback

# Manual rollback to a specific GitHub Release tag (e.g. v1.2.3).
# Use this when you need to recover from a bad deploy that passed the health check
# but caused issues in production (the auto-rollback in deploy.yml covers deploy-time failures).
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to roll back to (e.g. 1.2.3)'
        required: true
      service:
        description: 'Which service to roll back'
        required: true
        default: 'both'
        type: choice
        options:
          - both
          - api
          - web

jobs:
  rollback-api:
    name: Rollback API to v${{ inputs.version }}
    if: inputs.service == 'both' || inputs.service == 'api'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v${{ inputs.version }}

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy API at v${{ inputs.version }}
        run: railway up --service api
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      - name: Notify rollback
        if: always()
        run: |
          RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo "## ⚠️ Manual API rollback" >> "$GITHUB_STEP_SUMMARY"
          echo "API rolled back to **v${{ inputs.version }}** by @${{ github.actor }}." >> "$GITHUB_STEP_SUMMARY"
          echo "**Run:** $RUN_URL" >> "$GITHUB_STEP_SUMMARY"

          if [ -n "${{ secrets.SLACK_WEBHOOK_URL }}" ]; then
            curl -sf -X POST "${{ secrets.SLACK_WEBHOOK_URL }}" \
              -H "Content-Type: application/json" \
              -d "{
                \"text\": \"⚠️ *TwinTracker API manual rollback* to \`v${{ inputs.version }}\` by @${{ github.actor }}. <$RUN_URL|View run>\"
              }"
          fi

  rollback-web:
    name: Rollback Web to v${{ inputs.version }}
    if: inputs.service == 'both' || inputs.service == 'web'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v${{ inputs.version }}

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      - name: Deploy Web at v${{ inputs.version }}
        run: railway up --service web
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
